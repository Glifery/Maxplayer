define([
    'Utils/CheckType',
    'Utils/Debug',
    'backbone'
], function (
    CheckType,
    Debug,
    Backbone
    ) {

    var Domain = Backbone.Model.extend({

    });

    return Domain;
});